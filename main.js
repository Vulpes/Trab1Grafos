const index = require('./index')
const grafo = require('./CriaGrafo')
const fs = require('fs')
var content = fs.readFileSync('./grafo1.txt','utf-8')

/*let rep = grafo.grafo(2,content,'texto')
rep = index.lista.lista(9,content,'qualquer')*/
console.log(rep)
